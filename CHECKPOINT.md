# Checkpoint — Montador de Itens de LoL (Capítulo 2 · Forjador)
Última atualização: 13/09/2026 — FASE 6 CONCLUÍDA — CAPÍTULO 2 ENTREGUE (todas as fases aprovadas pelo Leo)

## 1. Objetivo geral
Evoluir o Montador de Itens (index.html) para a versão com identidade visual de loja medieval (Catálogo) e forja (Build), dados do Capítulo 1 (225 itens, patch 16.18.1) via data/catalog.js, sistema de áudio com lojista, e publicação no GitHub Pages — em fases aprovadas por captura de tela.

## 2. Concluído
- Montagem do agente (fora do Code): CLAUDE.md, docs/, data/catalog.js já gerado (225 itens), data/gerar_catalog_json.py, index.html da primeira tentativa como base.

### Fase 1 — Direção de arte
- T1 — Briefing lido (docs/direcao_de_arte.md, docs/PROMPT_Claude_Design_Fase1.md); Leo aprovou seguir sem handoff do Claude Design (13/09/2026). Texturas conferidas: as três são papel amassado quase branco, precisam de tingimento por blend.
- Fase 1, T2 — amostra em docs/amostras/fase1-amostra.html (captura `docs/screenshots/fase1-tarefa2-amostra.png`), duas direções lado a lado, lendo Quebrapassos, Bastão das Eras, Sinal de Sterak e Cristal de Rubi do catalog.js e as três texturas reais (tingidas por multiply sobre a cor do papel: 01 = card, 02 = busca/barra de filtros, 03 = placa de entrada). A = Taverna e pergaminho (papel #e5d3ad, tinta #2c1d12, madeira #3b2617, latão #b6873a, cera #8d2c22; títulos IM Fell English SC, texto Alegreya; selo de cera para tier, faixa lateral na cor da classe, chips como etiquetas de tinta/cera). B = Guilda e latão (papel #d9c59c, madeira #1d1713, latão #c9973f, bronze #7a5a1e; títulos Cinzel, texto Crimson Pro; etiqueta de latão para tier, borda superior na cor da classe, chips como placas de latão). Classe nas duas: AD laranja, AP roxo, Vitalidade verde. Forja igual nas duas (ferro #15110f, brasa #ff7a1e, moldes com rebites, faíscas, interruptores de brasa). Leo escolheu a Opção A em 13/09/2026. Commit "F1-T2".
- T3 — docs/direcao_de_arte.md escrito com a Opção A (paleta com códigos, classe, forja, tipografia, uso de cada textura, anatomia dos componentes, família de animações). FASE 1 CONCLUÍDA em 13/09/2026.
### Fase 8 — Builds publicadas
- T1 — Tela "Builds" sem servidor (14/09/2026, a partir dos três prints do Deadlock). Aba renomeada para **Builds**; título "Builds" com abas **Minhas** (ativa) e **Públicas** (desativada até o servidor); "+ Nova build", "Importar", **Filtrar por marcador** (painel com campeão do filtro — só os campeões presentes nas builds —, os 13 marcadores padrão, as habilidades, que só aparecem com um campeão escolhido, e **Itens** com os ícones dos 225 itens do catálogo e uma busca — Leo, 14/09/2026: "deve aparecer o ícone de todos os itens"; filtrar por item acha a build que tem o item como marcador ou em qualquer bancada) e **busca por nome ou ID**. Layout lista + detalhe: linha com retrato, nome, etiqueta "na forja", 3 marcadores e **coração de favorita** (local, `fav`); clique seleciona, duplo clique ou Enter abre a forja. Detalhe à direita: retrato e nome, os 3 marcadores com rótulo (vazios tracejados), **Versão 0 · ID não publicada · Última atualização** (`updatedAt`, só muda quando a build muda de verdade), descrição, **itens sem os opcionais**, botões Copiar e editar · Editar · Publicar (desativado até a T3) · Excluir, e a nota do que falta para publicar (itens, nome, descrição, 3 marcadores — regra do Leo). Campo **Descrição** na forja (textarea sob o nome, editável no modo Editar; obrigatória para publicar), persistida e no texto (`DESCRIÇÃO:` numa linha, "/" separa parágrafos; importado de volta). **Chuva de entrada**: ao abrir a lista, 34 pingos aleatórios revelam a tela por máscara de círculos crescendo (~0,9 s; JS sobre o DOM; respeita prefers-reduced-motion). Verificado no navegador embutido: 7 builds, seleção e detalhe (Ornn: 11 itens sem opcionais, 3 marcadores), filtro Ornn · AD → 1 build com as 4 habilidades no painel, busca por ID "b-captura" → 1, descrição salva/exportada/importada; 0 erros de console. Capturas `docs/screenshots/fase8-tarefa1-builds.png` e `fase8-tarefa1b-chuva.png`. Aprovado pelo Leo em 14/09/2026. Commit "F8-T1".
- T1b — Link com a build dentro, sem servidor (14/09/2026). "Copiar link" no resumo da tela Builds e no painel Exportar/importar: a build inteira (nome, modo, campeão, descrição, marcadores, caixas com itens, observações e o item forjado) vai em `#b=<base64url de JSON compacto>` (~1 KB para 15 itens). Quem abre o link entra na loja sem a placa, recebe a build na biblioteca com id derivado do conteúdo (`l-<hash>`, então abrir o mesmo link de novo não duplica) e cai na forja. Sem área de transferência, o link aparece selecionado numa caixa para Ctrl+C. Verificado no navegador embutido: link de 980 caracteres, build recebida com 3 caixas/15 itens/Ornn/descrição/3 marcadores, reabrir não duplica (8 builds antes e depois), fallback da cópia; 0 erros de console. Captura `docs/screenshots/fase8-tarefa1b-link.png`. Aprovado pelo Leo em 14/09/2026.
- T1c — Salvar na forja (Leo, 14/09/2026: "muito útil o botão de salvar, para salvar as alterações"). No modo Editar as alterações viram **rascunho**: `saveBuild()` só marca "alterações não salvas" (botão Salvar aceso, Descartar aparece); **Salvar** (ou Ctrl+S) grava com o som `build:save` e mostra "salvo"; **Descartar** volta ao último estado gravado. Sair com rascunho — desligar Editar, "‹ Builds", trocar de aba, trocar de build pelo seletor, Nova build, Duplicar — abre a placa "Alterações não salvas" com Continuar editando / Descartar / Salvar e sair; fechar a janela do navegador com rascunho pede confirmação (beforeunload). Fora da edição (modo, campeão, marcadores, interruptores) continua gravando na hora. Correção junto: a cópia da biblioteca e a build ao vivo compartilhavam o mesmo objeto de caixas, então o rascunho vazava para a cópia salva — agora `snapshotActive` e `activateBuild` copiam. Verificado no navegador embutido: adicionar item → não salvo (memória 16, disco 15); Descartar → volta a 15; Ctrl+S → disco atualizado e `build_open`; "‹ Builds" com rascunho → placa → Salvar e sair → lista com o disco atualizado; 0 erros de console. Captura `docs/screenshots/fase8-tarefa1c-salvar.png`. Aprovado pelo Leo em 14/09/2026.
- T2 — Servidor ligado (14/09/2026). Leo criou o projeto `montador-de-itens` no Supabase, rodou `docs/servidor/supabase.sql` e mandou a URL e a chave *publishable* (`data/servidor.js`, carregado depois do catálogo). A chave *secret* apareceu na conversa por engano: nunca vai para a página; pedido ao Leo para revogá-la. Duas correções no SQL depois dos testes: (1) `digest()`/`gen_random_bytes()` moram no esquema `extensions` no Supabase → `search_path = public, extensions` nas funções; (2) o verificador de segurança do Supabase marcou a view como SECURITY DEFINER → view com `security_invoker = true`, políticas de leitura pública em `builds` e `eventos` e `grant select` só nas colunas públicas de `builds` (o `segredo_hash` continua inacessível). Conferido pela API: view lê (200), tabela direto negada (401), função valida ("A build precisa de nome."), publicar/apagar de teste ok. Módulo `Servidor` (`ativo`, `rpc`, `get`, `segredo` aleatório por navegador em `localStorage publicacao.segredo`).
- T3 — Publicar (14/09/2026). Botão no resumo: validação local (itens, nome, descrição, 3 marcadores; senão o botão fica desativado com o motivo), placa "Publicando a build — Aguarde" (título com reticências animadas) e, no fim, "Build publicada com sucesso · ID · versão" (borda verde) ou "Não deu para publicar" com a mensagem do servidor (borda vermelha). A build guarda `pubId`, `pubVersao`, `pubEm`, `pubPatch`; o resumo mostra Versão N · ID, "Publicada: data", "desatualizada (patch)" quando o catálogo mudou, e a nota "Há alterações depois da última publicação" quando `updatedAt > pubEm`; o botão vira "Atualizar publicação" (versão +1, mesmo ID e segredo). Publicada em outro navegador ou apagada no servidor → vira publicação nova. Excluir uma build publicada também a apaga do site. Verificado no navegador embutido: Ornn publicada → ID 0857db versão 1; atualizar → versão 2, confirmado na view do servidor; 0 erros de console. Captura `docs/screenshots/fase8-tarefa3-publicada.png`. Aprovado pelo Leo em 14/09/2026 (T1b, T1c, T2 e T3 juntas). Commit "F8-T1b–T3". Manutenção futura documentada em docs/servidor/limpeza.sql e README (limpeza dos eventos à mão ou com pg_cron).
- T4 — Aba **Públicas** (14/09/2026). Lê até 200 builds da view `builds_publicas` (cache de 1 min; botão ↻ força) e ordena como o Leo pediu: **top 10 do dia** ("Popular hoje", laranja), **top 10 da semana** fora do top do dia ("Popular da semana", latão escuro), depois as **mais recentes**; seletor de quantidade 10/20/**50**/100 (guardado no navegador). Etiqueta **desatualizada** quando o `patch` da publicação difere do catálogo. Busca por nome ou ID (ID que não está na lista carregada é buscado no servidor); o filtro por marcador/campeão vale na aba, com os campeões vindos das publicadas. **Coração** marca favorita (lista local `publicacao.favoritos`); as favoritas aparecem também em **Minhas**, com etiqueta "favorita", buscadas por ID quando não estão na lista. Detalhe da pública: retrato, nome, marcadores com rótulo, **versão · ID**, última atualização, modo, "desatualizada", visualizações · cópias · hoje · na semana, autor, descrição, itens sem os opcionais, **Copiar e editar** (conta a cópia no servidor, cria a build local com nome, modo, campeão, marcadores, descrição e caixas, abre a forja) e Favoritar. Selecionar conta uma **visualização** (uma por sessão por build). Minhas: builds publicadas ganham a etiqueta "publicada" (ID · versão). Verificado no navegador embutido com 3 publicadas de teste (Ornn 0857db = Popular hoje; Tanque geral be03f1 com patch 16.17.1 = desatualizada; Jinx 572b48): ordem, etiquetas, favoritar → aparece em Minhas, busca por ID, quantidade 10, Copiar e editar → build local completa e `copias` = 1 no servidor; 0 erros de console. Captura `docs/screenshots/fase8-tarefa4-publicas.png`. As três publicações de teste saíram do servidor em 14/09/2026, depois do teste do Leo. Aprovado pelo Leo em 14/09/2026.
- T5 — Revisão quando o Capítulo 1 muda (Leo, 14/09/2026: "a tag desatualizada sempre fica ativa quando o Capítulo 1 atualiza, preciso de uma forma simples de atualizar, e a build contém itens com status antigos ou removidos"). Fatos: atributos e preços nunca ficam velhos — tudo é lido do catalog.js atual a cada render; o que pode acontecer é um item sair do catálogo. Implementado: cada build guarda o `patch` do catálogo da última gravação e o **nome** de cada item junto do slug (só para a revisão saber dizer o nome de quem saiu). Ao carregar, se o patch gravado difere do atual ou algum item não existe mais, a build ganha `revisao = {de, para, removidos:[{nome, caixa}]}`: etiqueta **desatualizada** em Minhas, caixa vermelha no resumo ("Catálogo mudou: 16.17.1 → 16.18.1. Atributos e preços já são os atuais. Saíram do catálogo e foram tirados da build: …") e a mesma placa na forja. Leo (14/09/2026): "não precisa marcar como revisado, é só publicar novamente" → sem botão: **Atualizar publicação** fecha a revisão e tira a etiqueta do site (o servidor recebe o patch novo); numa build só local, **Salvar** na forja fecha o aviso. Copiar uma pública de catálogo antigo avisa quais itens ficaram de fora. Verificado no navegador embutido simulando patch 16.17.1 + item inexistente: etiqueta, caixa com "Item Antigo (Núcleo)", aviso na forja, Atualizar publicação / Salvar limpam e persistem; 0 erros de console. Captura `docs/screenshots/fase8-tarefa5-revisao.png`. Aprovado pelo Leo em 14/09/2026. Commit "F8-T4–T5".

### Fase 7 — Build no cliente do LoL
- T1 — Exportar a build como conjunto de itens do cliente do LoL (Leo, 14/09/2026: "a opção 1 parece mais adequada… se conseguir encaminhar para a pasta para apenas colar o arquivo seria de muito agrado"). No painel Exportar/importar, bloco "Cliente do LoL · conjunto de itens" com **Copiar para o cliente** (JSON na área de transferência → Coleção → Itens → Importar) e **Salvar arquivo .json** (janela nativa "Salvar como" via showSaveFilePicker, que deixa apontar direto a pasta `Config\Global\Recommended\`; sem a API, download comum com aviso para mover). `buildToItemSet()`: `title` = nome da build; cada categoria vira um bloco `"<Tipo> · <nome>"` com `items[{id, count}]` na ordem da bancada (repetição vira `count`); `associatedMaps`/`associatedChampions` vazios (todos os mapas e campeões — o catálogo não tem campeão); chaves antigas `type/map/mode/priority/sortrank` para a versão em arquivo. Só entra o `id` numérico do catálogo: o Atributo adicional (id "6032 (a wiki registra…)") fica de fora com aviso. Observações, Mestre Forjador, Ápice e Reembolso não existem no formato do cliente (a placa avisa). Verificado no navegador embutido: 3 blocos, ids 6631/8020/6676/3075/3039, nome de arquivo "Build de captura.json", 0 erros de console; a cópia automática cai na caixa selecionada quando o navegador nega a área de transferência. Formato conforme hextechdocs.dev/item-sets e o fluxo de importação do cliente; o teste real no cliente é do Leo. Captura `docs/screenshots/fase7-tarefa1-cliente-lol.png`. Aprovado pelo Leo em 14/09/2026 ("perfeito, vamos dar continuidade"). Commit "F7-T1". Conferência com o cliente (Leo, 14/09/2026, Arsenal.json exportado do cliente + texto do Montador da mesma build de Ornn): 21 de 21 itens com o mesmo id (Botas Galvanizadas 3047 … Jak'Sho 6665; Poção de Vida ×2 vira count 2); o cliente grava só `title`, `associatedMaps`, `associatedChampions` (516 = Ornn) e `blocks[{items, type}]` → o Montador passou a gravar exatamente essas chaves (as antigas type/map/mode/priority/sortrank saíram) e o nome do bloco é o nome da categoria, com o sufixo " - PRIORIDADE" / " - Opcional" (Leo, 14/09/2026). Ajuste feito junto com a T3/T4.
- T2 — Campeão da build via **Data Dragon** (Leo, 14/09/2026: "não é bom usar o Data Dragon da Riot?" — sim, é a fonte). Módulo `Campeoes`: busca `cdn/<CATALOG.meta.patch>/data/pt_BR/champion.json` (16.18.1 → 173 campeões, nome pt-BR, `key` numérica, `id` da pasta), guarda no navegador por versão (`localStorage campeoes.<patch>`), retrato em `cdn/<patch>/img/champion/<id>.png`; nada digitado à mão; sem internet na primeira vez o seletor avisa e a build fica em "Todos os campeões". `build.champion = {key, id, name}` persistido na biblioteca (snapshot/activate/createNewBuild). UI: seletor "Campeão" na barra da biblioteca ao lado de Modo, com ícone redondo; retrato de 46 px antes do nome da build; no bloco Cliente do LoL a pasta muda para `Config\Champions\<id>\Recommended\` e o JSON leva `associatedChampions: [key]` (Jinx → 222). Texto exportado ganha `CAMPEÃO: <nome>`; importar resolve pelo nome do Data Dragon (Ahri → ok; "Fulano" → aviso e fica em Todos). Verificado no navegador embutido e no Chrome real (retrato carregado), 0 erros de console. Ajuste (Leo, 14/09/2026: "a fonte da caixa de seleção tá muito clara, não dá pra ver nada"): a lista aberta dos `<select>` da forja era desenhada pelo sistema (texto claro sobre fundo claro) → `color-scheme: dark` e opções com fundo de ferro #1f1813, tinta da forja e a escolhida em brasa; vale para Modo, Campeão e Suas builds. Ajuste 2 (Leo, 14/09/2026: "acho bom ter os ícones dos campeões também, pois tem muito campeão que só é reconhecido pelo ícone"): o `<select>` nativo não mostra imagem → seletor próprio: botão com retrato + nome, painel de ferro com busca (nome ou id do Data Dragon, sem acento/caixa; Enter escolhe o primeiro), lista em 3 colunas com o retrato de cada campeão (`loading="lazy"`), "Todos os campeões" no topo, Esc e clique fora fecham; `setBuildChampion(c)` centraliza a troca. Verificado: 173 retratos + Todos, busca "ka" → 14, Kai'Sa → key 145 e pasta `Champions\Kaisa\Recommended\`, Enter em "jinx" → Jinx. Capturas `docs/screenshots/fase7-tarefa2-campeao.png` e `fase7-tarefa2b-seletor-campeao.png`. Aprovado pelo Leo em 14/09/2026. Commit "F7-T2".
- T3 — Aba Build em duas telas, como os Arsenais do cliente do LoL (Leo, 14/09/2026, com prints do cliente: "seria melhor como é no LoL"; a primeira versão, uma prateleira de placas, foi substituída no mesmo dia). **Tela 1 — Arsenais** (padrão ao abrir a aba): título, "+ Nova build", "Importar" (abre a forja com o painel de importar), busca por nome/campeão/modo; tabela de ferro com Nome, Campeão (retrato do Data Dragon ou ∞), Modo, Itens e ações ⧉ duplicar / ✕ excluir; a ativa marcada com friso de brasa; clique (ou Enter) abre a forja. **Tela 2 — a forja** de sempre, com "‹ Arsenais" para voltar e, ao lado do nome da build, um seletor ▾ que lista as builds (retrato, nome, itens · modo) para trocar sem sair. `buildScreen` guarda a tela na sessão; atalhos `#build*` abrem direto na forja (links e capturas). `duplicateBuild(id)` leva modo e campeão; `deleteBuild` de uma build que não é a ativa não troca a ativa. Correção junto: `loadBuild()` remontava as builds sem `champion` e o campeão se perdia a cada recarga — preservado. Verificado no navegador embutido: lista com 3 builds, busca "ahri" → 1, clique abre a forja, seletor troca a build, "‹ Arsenais" volta com a ativa marcada, duplicar cria a cópia e volta à lista; 0 erros de console. Capturas `docs/screenshots/fase7-tarefa3-arsenais.png` e `fase7-tarefa3b-forja-seletor.png`. Aprovado pelo Leo em 14/09/2026. Commit "F7-T3".
- T4 — "Importar" aceita também o Arsenal.json do cliente (sentido inverso da T1; proposto ao Leo ao fechar a T3). Texto que começa com "{" é lido como JSON: `itemSetToBuild()` — `title` vira o nome, cada bloco vira uma categoria (o sufixo " - PRIORIDADE" / " - Opcional" gravado pelo Montador volta a ser o tipo da caixa, aceitando também "·"; blocos do cliente entram como Comum), `count` N repete o item N vezes, ids fora do catálogo são listados no aviso, `associatedChampions` → campeão pela chave do Data Dragon, `associatedMaps` 11/12 → Summoner's Rift/ARAM (tabela `MAPA_MODO`, constante da Riot, não dado de item). Verificado no navegador embutido com o Arsenal.json do Ornn: 5 blocos, 21 itens (Poção de Vida ×2), Ornn, e ida-e-volta idêntica (importar → Copiar para o cliente reproduz blocos, ids e counts do arquivo original). 0 erros de console. Ajuste (Leo, 14/09/2026): sufixo dos blocos no cliente passa a ser " - PRIORIDADE" / " - Opcional". Captura `docs/screenshots/fase7-tarefa4-importar-arsenal.png`. Aprovado pelo Leo em 14/09/2026. Commit "F7-T4". FASE 7 CONCLUÍDA nos dois sentidos (Montador → cliente e cliente → Montador); só a pedido: envio direto ao cliente por programa local.
- T5 — Marcadores da build, até 3, como os marcadores dos guias do Deadlock (Leo, 14/09/2026, com o print "Selecionar marcador" e os 14 SVGs colocados em `assets/Icon/panorama/images/builds`). Três grupos: **Marcadores padrão** (13, com os rótulos do Leo nos termos do LoL: AD, Vitalidade, AP, Para novos/intermediários/avançados, Dano, Utilidade, Cura, Controle de grupo, Mobilidade, Burst, Debuff — o 14º SVG, headshots, ficou fora por não estar na lista), **Habilidades** Q/W/E/R do campeão da build sem a passiva (Data Dragon `data/pt_BR/champion/<id>.json` + `img/spell/`, guardado por versão), **Itens no guia** (ícones dos itens da build, sem repetir). Na forja, três espaços ao lado do nome; clique abre o diálogo de ferro "Selecionar marcador" (Esc, × ou fora fecham; "Limpar este espaço"; × no espaço remove). Na lista de Arsenais a coluna Itens virou **Marcadores** (a contagem foi para o título da célula e para o seletor da forja). Forma `{t:"tag",id}|{t:"hab",letter}|{t:"item",slug}`, persistida na biblioteca (snapshot/activate/loadBuild/createNewBuild) e no texto (`MARCADORES: AD | Habilidade Q | Gume do Infinito`, importado de volta). Verificado no navegador embutido com a build do Ornn: 13 + 4 (Ruptura Vulcânica…) + 14 células, três espaços preenchidos, texto de ida e volta igual, lista com os 3 ícones; 0 erros de console. Capturas `docs/screenshots/fase7-tarefa5-selecionar-marcador.png` e `fase7-tarefa5b-arsenais-marcadores.png`. Aprovado pelo Leo em 14/09/2026. Commit "F7-T5". FASE 7 CONCLUÍDA.
### Fase 6 — Publicação e QA
- T1 — Decisão do Leo (13/09/2026): "gostei da ideia, só acho muito alto esse áudio" → `ui_catalog_tag_select` toca ao abrir o pergaminho do item (evento `catalog:pergaminho`, cooldown 120 ms) e continua nos chips, ambos com ganho 0,5 sobre o volume geral (`tocar(..., {ganho})`). Verificado: pergaminho e chip a 0,40 com volume 0,80; hover a 0,80. Mapa de áudio atualizado. Commit "F6-T1".
- T2 — Leo testou no Chrome real (13/09/2026): "pelo que parece tá rodando tudo muito bem".
- T3 — QA no Pages (13/09/2026): 0 caminhos absolutos "/assets" ou "/data" no index.html; referências locais (3 texturas, data/catalog.js) existem no disco com caixa exata; no Pages, .nojekyll, catalog.js (application/javascript) e as 3 texturas (image/png) respondem 200; 82 áudios com 200 (Fase 4); DOM do site publicado com 225 cards, módulo Som e forja via #build. Commit "F6-T3".
- T4 — docs/atalhos.md (links diretos e atalhos de captura, ajustes de console), docs/mapa_de_audio.md e docs/direcao_de_arte.md conferidos com o código; CHECKPOINT no estado final. FASE 6 CONCLUÍDA em 13/09/2026 — CAPÍTULO 2 ENTREGUE.
- T5 — Correção (Leo, 14/09/2026: "falta o drag and drop de um item que já está em uma categoria para outra"). Causa: o molde arrastado anunciava `effectAllowed = "move"` mas o `dragover` pedia `dropEffect = "copy"` (só a categoria pedia "move"); no Chrome real, quando o dropEffect não cabe no effectAllowed, o `drop` não dispara — a bancada acendia e nada acontecia ao soltar. Eventos sintéticos não reproduziam; reproduzido com arraste nativo via CDP (Input.setInterceptDrags + dispatchDragEvent, script no scratchpad). Correção: dropEffect "move" para categoria e para molde vindo de bancada, "copy" só do seletor; `moveItem` ignora soltar na própria bancada no fim e toca `build:item_move` (mesmo `build_item_add_01..04`, sem fala do lojista). Verificado no Chrome real: molde → outra bancada (pelo ícone e pela borda), seletor → bancada e reordenar categoria, 0 erros de console. Captura `docs/screenshots/fase6-tarefa5-arraste-entre-bancadas.png`.
  Segunda rodada (Leo, 14/09/2026: "ainda não consigo segurar os itens e soltar" + "os sons de add na build valem também para itens adicionados pelo catálogo"): (1) em **visualização** (padrão) os moldes não são arrastáveis por decisão da Fase 3 — agora tentar puxar um molde acende o interruptor "Editar" (duas pulsações de brasa) e mostra a placa "A build está em visualização — ligue Editar para mover os moldes" por 3,2 s, sem mexer na build (captura `docs/screenshots/fase6-tarefa5b-chamar-editar.png`); (2) marcar um item pelo Catálogo (categoria selecionada) toca `build:item_add` com as falas do lojista, sempre o som genérico — Leo (14/09/2026) não queria o som de Lendário fora da forja (`ctx.origem = "catalogo"`); desmarcar toca o clique da loja. Verificado no navegador embutido: Lâmina Cintilante Navori (Lendário) marcada → `build_item_add_0N.mp3`; desmarcada → `ui_shop_click_danodeataque.mp3`. Aprovado pelo Leo em 14/09/2026 ("eu pensei que ia usar os sons dos lendários, mas você fez da forma que eu pensei mesmo, porque não queria que fosse o som dos lendários" → o Catálogo toca sempre o genérico, `ctx.origem = "catalogo"`). Commit "F6-T5".

### Fase 5 — Calculadora e biblioteca
- Fase 5, T1 — Calculadora como pergaminho de contas (captura `docs/screenshots/fase5-tarefa1-calculadora.png`) — aprovado pelo Leo em 13/09/2026, commit "F5-T1". Bloco CSS "CALCULADORA": a vista inteira vira um pergaminho (papel com textura sorteada, borda e sombra da loja), título em IM Fell, rótulos em Cinzel, campos em papel com foco em cera, botão "Puxar da build ativa" em cera, cards de Dano físico (laranja do AD) e Dano mágico (roxo do AP) com barras, valores em IM Fell, "você toma" em cera e "absorvido" em verde, veredito em placa com faixa colorida; entra descendo como a loja. Fórmulas intocadas. "Puxar da build ativa" passa a somar os atributos efetivos (`effectiveAttributes`: Mestre Forjador/Ápice), só valores fixos; preços de referência seguem lidos do catálogo (Cristal de Rubi, Pano, Cota, Nulificador, Negatron) e agora em pt-BR (2,67g). Atalho `#calc`. Testado no navegador embutido: Armadura de Espinhos + Cristal de Rubi → 300 de Vida / 75 de Armadura; com Mestre Forjador → 485 / 100 (masterwork.stats do catálogo); console limpo.
- Fase 5, T2 — biblioteca e exportar/importar (captura `docs/screenshots/fase5-tarefa2-exportar.png`) — aprovado pelo Leo em 13/09/2026, commit "F5-T2". Texto exportado ganha a linha "MESTRE FORJADOR: sim" (depois de MODO); a importação lê essa linha e liga o interruptor na build criada. Formato documentado no comentário do código. Teste de ida e volta no navegador embutido: build "Jinx ARAM" (modo ARAM, MF ligado, Prioridade ½ com descrição e observação, Opcional) → texto → parse restaura nome, modo, MF, tipos, larguras, descrição, itens pelo nome PT e observação; importação por nomes EN com [OBRIGATÓRIA] → prioridade ⅓, item inexistente listado em "não encontrados", Botas Levemente Mágicas resolvida; botão Importar cria a build, ativa, mensagem "Importado: 2 categoria(s), 3 item(ns), modo ARAM.", interruptor MF marcado. Biblioteca (nova, duplicar, excluir, trocar) e migração de chaves antigas já cobertas nas fases anteriores. Atalho `#build-exportar` (captura).
- Fase 5, T2b — Mestre Forjador em UM item só (correção do Leo: "o mestre forjador só aprimora 1 item"; captura `docs/screenshots/fase5-tarefa2b-mestre-forjador-um-item.png`) — aprovado pelo Leo em 13/09/2026, commit "F5-T2b". O interruptor da build agora só habilita a escolha: com ele ligado, os Lendários elegíveis mostram a placa "MF" apagada (`mf-elegivel`); na EDIÇÃO, clicar no ÍCONE do molde forja aquele item (`entry.mf = true`, salvo na build) e desforja o anterior (segunda correção do Leo: "clique no ícone na edição"); clicar de novo desforja; item não elegível não forja; a placa MF é só indicador (apagada = elegível, acesa = forjado) e o ícone elegível ganha cursor e brilho de latão ao passar o mouse na edição. Só o forjado recebe `masterwork.stats` no molde (placa acesa, anel de latão), no tooltip ("forjado" / "elegível, clique na placa MF"), nos totais da caixa e da build (nota diz "Mestre Forjador em <item>" ou pede a escolha) e no "Puxar da build" da Calculadora. Exportar grava "MESTRE FORJADOR: <nome do item>" e importar forja esse item ("sim" só liga o interruptor). Testado no navegador embutido: ligado sem escolha → Gume e Rabadon elegíveis, total +75 DdA; forjar Gume → +103,57; forjar Rabadon desforja o Gume; clicar de novo desforja; Cristal de Rubi não forja; export/import com o nome; console limpo.
- Fase 5, T3 — aviso por modo de jogo (captura `docs/screenshots/fase5-tarefa3-aviso-modo.png`) — aprovado pelo Leo em 13/09/2026, commit "F5-T3". Forja: o `mode-warning` existente (item fora do modo escolhido → placa de brasa "1 item não está disponível em <modo>: <item>"; build sem modo misturando modos → aviso de conflito) já estava no tema; molde fora do modo fica cinza. Catálogo: ao marcar um item que não existe no modo da build ativa, faixa em papel/cera abaixo das abas ("<item> não existe em <modo>, o modo da build <nome>. Foi marcado mesmo assim; na forja ele aparece apagado."), some em 5 s; item do modo não avisa. Testado no navegador embutido: build "Build SR" em Summoner's Rift, marcar Acerto de Contas de Atma (só ARAM: Mayhem) → faixa + item marcado; Gume não avisa; forja mostra a placa de erro e 1 molde apagado; console limpo.
- Fase 5, T4 — QA: no servidor local, 225 cards, build criada na edição persiste após recarregar (nome, 2 itens, contagem na aba), Calculadora puxa da build (Cristal de Rubi → 150 de Vida), console sem erro (só o confirm() do "Limpar build" suprimido pelo navegador embutido); do disco (Chrome headless) Calculadora renderiza e a entrada fecha por link direto. FASE 5 CONCLUÍDA em 13/09/2026.

### Fase 4 — Som e lojista
- Fase 4, T1 — módulo `Som` e controle no cabeçalho (captura `docs/screenshots/fase4-tarefa1-som.png`) — aprovado pelo Leo em 13/09/2026, commit "F4-T1..T5". Novo `<script>` logo após o catalog.js: `SOM_ARQUIVOS` (27 eventos-base → 82 arquivos) gerado da lista real de assets/audio (scratchpad, nenhum nome digitado); `Som.play(evento, contexto)` traduz os eventos do mapa (catalog:open/close, build:open/close, catalog:hover/click/tag_select, lojista:tag_open/efeito_select, build:item_add com Lendário por núcleo + fala rara por nameEn) para as bases de arquivo, com núcleo de `itemClass.core` (Garrabrasa sorteia AD/AP), tiers neutros para o clique, sorteio sem repetir a última variante, cooldown por grupo (hover 150 ms, clique e chip 80 ms), fala do lojista descartada se outra estiver tocando, volume e mudo em localStorage (`som.volume`, `som.mudo`), `Som.destravar()` reservado ao clique de entrada (nada toca antes), `Som.verificar()` para QA. Único lugar do app com `new Audio()`. Controle no cabeçalho: sino de latão (brasa na forja) alterna mudo, régua de volume; refletem o estado. Testado no navegador embutido: play antes de destravar = false; depois hover AD = true e segundo hover no cooldown = false; clique em Sentinela = neutro; segunda fala do lojista descartada; Lendário no Build = true; mudo/volume persistem; `Som.verificar()` = 82 ok, 0 falhas; console limpo.
- Fase 4, T2 — telas: o clique em "Entrar na loja" chama `Som.destravar()` e toca open_catalog; entrar na forja toca close_catalog + build_open (6 variantes); sair da forja toca build_close + open_catalog. Links diretos destravam sem tocar.
- Fase 4, T3 — Catálogo: hover no card toca ui_catalog_mod_hover_<núcleo> (cooldown 150 ms), clique no card toca ui_shop_click_<núcleo> ou o neutro (Consumível/Distribuído/Trinket), clique em chip toca ui_catalog_tag_select. Verificado no navegador embutido com um espião em `Audio`: open_catalog → hovers com variantes diferentes (02, 11, 07, 04) → tag_select_06 → close_catalog + build_open_04 → build_close + open_catalog. O Leo relatou "não estou ouvindo nada" antes destas ligações (esperado: a T1 só criou o módulo). Aprovado pelo Leo em 13/09/2026 (commit "F4-T1..T5").
- Fase 4, T3b — ajustes após o Leo ouvir (13/09/2026): (1) "parece ter delay" → todos os 82 arquivos são pré-carregados em `Som.destravar()` (cache de Audio com preload=auto; ao tocar usa o elemento pronto, ou um clone se ainda estiver tocando) e o hover perdeu o cooldown: toca em todo card que o mouse tocar; (2) regra explicitada no módulo e no docs/mapa_de_audio.md: sempre que um evento tem mais de um arquivo, a ativação é aleatória (sorteio a cada disparo, sem repetir o anterior). Verificado no navegador embutido: seis hovers seguidos disparam os seis, em 0,6 ms.
- Fase 4, T4 — lojista no Catálogo: `shopkeeper_tag_open_<núcleo>` ao TROCAR para a aba AD/AP/Vitalidade (confirmado pelo Leo: "sim, trocar"; Todos e a mesma aba não falam); `shopkeeper_build_efeitosespeciais_<efeito>_select` ao marcar os chips Lentidão e Dano ao Longo do Tempo (desmarcar não fala). Mapa de áudio atualizado.
- Fase 4, T5 — forja: `addItemToCategory` chama `Som.play("build:item_add", {item})` → build_item_add (4 variantes) para não Lendário, ui_build_mod_add_legendary_<núcleo> para Lendário, mais a fala rara shopkeeper_build_add_<item> quando existe (6 itens). Verificado no navegador embutido com espião no play(): aba AP → tag_open_poderdehabilidade_02; Vitalidade → tank_01; Todos e mesma aba → nada; Lentidão → tag_select_03 + lentidao_select_01; Dano ao Longo do Tempo → danoaolongodotempo_select_02; Cristal de Rubi → build_item_add_01; Gume → legendary_danodeataque + fala infinityedge; Rabadon → legendary_poderdehabilidade + fala; Coração de Aço → legendary_tank + fala; Garrabrasa (AD e AP, Starter) → build_item_add_04. Checagem estática: 27 bases do mapa, 0 sem gatilho. Aprovado pelo Leo em 13/09/2026.
- Fase 4, T5b — regras do lojista (Leo, 13/09/2026: "não reseta a cada acionamento… meio segundo de acréscimo… falar de vez em quando"): a fala em curso nunca é cortada; nova fala só após a atual terminar (evento ended) + 500 ms de folga; falas de aba e de efeito passam por 40% de chance e intervalo mínimo de 15 s entre falas; falas raras por item sempre tocam (sem chance), respeitando fila e folga. Constantes ajustáveis em `Som.LOJISTA` (FOLGA_MS 500, INTERVALO_MIN_MS 15000, CHANCE 0.4). Mapa de áudio atualizado. Verificado no navegador embutido com espião: três trocas de aba seguidas → 1 fala; logo após terminar → bloqueada pela folga; dentro dos 15 s → bloqueada; com intervalo zerado → fala; fala rara com chance 0 → toca.
- Fase 4, T5c — Leo (13/09/2026): "shopkeeper_tag_open_* não tocam ao adicionar itens à build" → ao entrar um item na bancada, se ele não tem fala rara própria, o lojista comenta pelo núcleo do item (shopkeeper_tag_open_<núcleo>), sujeito a chance/intervalo/folga; a troca de aba continua valendo. Mapa de áudio atualizado.
- Fase 4, T5d — Leo (13/09/2026): shopkeeper_build_efeitosespeciais_* ativa ao adicionar à build qualquer item com o efeito (Dano ao Longo do Tempo: 11 itens; Lentidão: 10). Prioridade na entrada: fala rara do item > fala do efeito > comentário do núcleo (uma só, nas regras do lojista). O gatilho no chip do efeito foi mantido. Mapa de áudio atualizado. Pergunta do Leo sobre ui_catalog_tag_select ("ativa quando abre os detalhes do item?"): hoje toca nos chips; abrir o pergaminho toca o clique do núcleo — ofereci mover para a abertura do pergaminho, aguardando resposta.
- Fase 4, T6 — QA de áudio: commit "F4-T1..T5" publicado (0a753f5), Pages atualizado em 30 s; HEAD nos 82 arquivos em https://dksaxton.github.io/montador_de_itens_lol/assets/audio/ → 82 × 200 (audio/mp3), 0 falhas; no disco e no servidor local `Som.verificar()` = 82 ok; 27 eventos com gatilho, 0 órfãos; mudo e volume persistem. Pendência de decisão do Leo: mover `ui_catalog_tag_select` para a abertura do pergaminho (hoje nos chips). FASE 4 CONCLUÍDA em 13/09/2026.

### Fase 3 — Build (forja)
- Fase 3, T1 — esqueleto da forja (captura `docs/screenshots/fase3-tarefa1-esqueleto-forja.png`) — aprovado pelo Leo em 13/09/2026, commit "F3-T1". Bloco CSS "FORJA" com variáveis --forja-*: ao entrar na aba Build o body ganha a classe `forja` (fundo de ferro com brasa subindo do rodapé, cabeçalho em ferro, aba ativa e contagem em brasa) e a vista sobe (animação `forja-sobe`); ao sair volta à madeira e a loja (ou a Calculadora) desce clareando (animação `loja-desce`, pedido do Leo: a volta também tem que acontecer); fundo e cabeçalho com transição de .5s. Barra da build, biblioteca e painel exportar/importar como placas de ferro com rebites; nome da build em IM Fell sobre linha tracejada com foco em brasa; botões de ferro (primário em brasa), seletor de largura com ativo em brasa, selects e textarea de ferro, aviso de modo em placa com brasa. Atalho `#build` abre direto na forja. Tiles, categorias e picker continuam no estilo anterior (T2). Ganchos comentados para Som.play("build:open"/"build:close"). Testado no navegador embutido: classe forja e animação ao entrar, fundo #100d0b, IM Fell no nome, volta à madeira ao sair, console limpo.
- Fase 3, T2 — bancadas, moldes, tooltip e picker (captura `docs/screenshots/fase3-tarefa2-moldes.png`) — aprovado pelo Leo em 13/09/2026, commit "F3-T2". Categorias como bancadas de ferro (placa com sombra interna, borda superior na cor da caixa: Comum ferro claro #9a8875, Prioridade brasa, Opcional aço frio #7f8a96; selecionada acende em brasa; alvo de arrasto acende), cabeçalho em barra escura com nome em IM Fell, botões de placa (tipo, fechar) em Cinzel, descrição em itálico tracejado, total em placa com número em brasa. Tiles como moldes: quadrado de ferro radial com rebites, contorno #6a3a18 e glow de brasa, ícone com moldura #7a4a20, nome em Alegreya, hover levanta e acende, fora do modo fica cinza. Tooltip do item em placa de ferro com borda de brasa. Picker lateral em papel de balcão (textura sorteada), linhas com faixa do tier, nome em tinta, preço em cera. Testado no navegador embutido com duas bancadas (Prioridade e Opcional) e quatro moldes, tooltip ao passar o mouse, console limpo.
- Fase 3, T3 — modos visualização/edição (captura `docs/screenshots/fase3-tarefa3-visualizacao.png`) — aprovado pelo Leo em 13/09/2026 (seguiu para a T4), commit "F3-T3". `build.editing` (não persiste; visualização é o padrão ao abrir) e interruptor de brasa "Editar" na barra da build. Visualização: sem alça de arrastar, botões fechar/redimensionar, seletor de largura, "Nova categoria", "Limpar build" e picker; nome e descrição das caixas fixos (descrição vazia some); tipo da caixa é só rótulo; moldes não arrastáveis, sem duplo clique/botão direito; tooltip diz "Ligue Editar para mudar"; item com observação ganha contorno de brasa clara; bancadas ocupam a largura toda; build vazia mostra "Ligue Editar para criar categorias". Edição libera tudo. Biblioteca, modo de jogo e exportar/importar nos dois modos; a marcação pelo "Marcar em" do Catálogo continua independente (é uma escolha explícita). Testado no navegador embutido: visualização esconde picker/alças/botões, 1 coluna, moldes não arrastáveis; edição mostra tudo, 2 colunas, arrastáveis; console limpo.
- Fase 3, T4 — drag-and-drop animado, "placa de pedra caindo" (captura `docs/screenshots/fase3-tarefa4-placa-caindo.png`, quadros congelados via `#build-animacoes`) — aprovado pelo Leo em 13/09/2026, commit "F3-T4". Pegar: o molde arrastado fica apagado e encolhido (lugar vazio). Soltar (do picker, do Catálogo via "Marcar em" ou movendo entre bancadas): `lastDrop` marca o item e a renderização seguinte anima só ele — a placa cai de 46px acima em escala 1.35 com sombra, bate (achata), quica, o contorno acende em brasa e o ícone solta uma baforada (`pedra-cai` + `brasa-baforada`, 600 ms); a bancada treme (`bancada-treme`). Remover (botão direito na edição): a placa escurece e afunda (`pedra-some`, 380 ms) antes de sair do modelo. Bancada alvo do arrasto ganha brasa no fundo. Respeita prefers-reduced-motion. Ganchos comentados para Som.play("build:item_add"/"build:add_legendary"). Testado no navegador embutido: adicionar pelo picker (classe tile-drop + cat-impact, animação pedra-cai), mover entre bancadas (anima no destino), remover (pedra-some e depois some do modelo e do DOM); console limpo (os erros de prompt() no log são da página anterior, do duplo clique da T3).
- Fase 3, T5 — caixas customizáveis (captura `docs/screenshots/fase3-tarefa5-caixas.png`) — aprovado pelo Leo em 13/09/2026, commit "F3-T5". Os três tipos existentes viram placas de metal: Comum = ferro, Prioridade = latão, Opcional = aço; a borda superior da caixa Prioridade também é latão (#c9973f) — a brasa pura "dá desconforto nos olhos" (Leo, 13/09/2026); a brasa fica só para ações (botão principal, molde aceso, aba ativa); clique na placa alterna o tipo só na edição (na visualização é rótulo). Alça de redimensionar em bronze, brasa ao passar/arrastar. Arrastar, redimensionar, renomear e descrever só na edição (T3). Importar aceita "[OBRIGATÓRIA]"/"[OBRIGATORIO]" como Prioridade (nome usado no briefing do CLAUDE.md); o rótulo "Prioridade" foi mantido porque o HTML anterior já tinha migrado "obrigatoria" → "prioridade" (decisão anterior do Leo) — renomear é uma linha, se ele quiser. Testado no navegador embutido: três placas com os gradientes certos, clique alterna na edição e não na visualização, importação com [OBRIGATÓRIA] → prioridade; console limpo.
- Fase 3, T6 — observação por item na própria tela (captura `docs/screenshots/fase3-tarefa6-observacao.png`) — aprovado pelo Leo em 13/09/2026, commit "F3-T6". O window.prompt saiu: duplo clique no molde (só na edição) abre uma plaquinha de ferro com borda de brasa ancorada ao molde (abaixo, ou acima se não couber), com o nome do item, textarea e botões Cancelar / Apagar / Salvar; Enter salva, Shift+Enter quebra linha, Esc cancela, clique fora fecha sem salvar. A observação continua no tooltip ao passar o mouse nos dois modos e o molde ganha o ponto de brasa (edição) ou contorno claro (visualização). Atalho `#build-observacao` (captura). Testado no navegador embutido com eventos: abre com foco no texto, Enter salva no modelo e no localStorage, tooltip mostra, Esc cancela, na visualização o duplo clique não abre; console limpo (o teclado do painel embutido não chega à página — teste por eventos sintéticos).
- Fase 3, T7 — interruptor Mestre Forjador (captura `docs/screenshots/fase3-tarefa7-mestre-forjador.png`) — aprovado pelo Leo em 13/09/2026, commit "F3-T7". Interruptor de brasa na barra da build, ao lado do Editar; `build.masterwork` é salvo na build (snapshot/biblioteca) e o seletor reflete a build ativa. Ligado: itens com `masterwork.eligible === true` e `stats` ganham anel de latão, moldura clara e placa "MF" no molde; o tooltip mostra cada atributo "base (+bônus) = total" lido de `masterwork.stats`; desligado, o tooltip ainda lista os valores marcados "(desligado)". `mfAttributes(item)` devolve os atributos com o total do catálogo no lugar do base (para os totais da T9). Testado no navegador embutido: Gume, Muramana e Coletora acendem, Cristal de Rubi não; tooltip "Dano de Ataque: 75 (+28,57) = 103,57"; salvo no localStorage; nova build nasce desligada; console limpo.
- Fase 3, T8 — interruptores Reembolso e Ápice na forja (captura `docs/screenshots/fase3-tarefa8-reembolso-apice.png`) — aprovado pelo Leo em 13/09/2026, commit "F3-T8". Dois interruptores de brasa na barra da build espelhando as caixas do Catálogo (mesmo estado global `state.applyCashback`/`applyApex`: ligar num lado liga no outro, faixa de aviso inclusa). Reembolso: moldes, totais das bancadas, resumo e picker usam `cashback.netGold` (picker marca "↓"), tooltip diz "com Reembolso (de Xg)". Ápice: molde com ápice ganha placa "ÁPICE" e brilho, tooltip lista os atributos do ápice no lugar do base (só os que mudam; texto do ápice quando nada é extraível). Placa MF foi para a esquerda e ÁPICE para a direita do molde. Ouro agora formatado em pt-BR (`fmtG`: 3.237,5g) no Catálogo, picker, bancadas, resumo e tooltip. Atalho `#build-apice` (captura). Testado no navegador embutido: Gume 3.237,5g com Reembolso, total 3.587,5g, Coletora 2.775g no Catálogo, Lacre Sombrio no ápice mostra só "+55 de Poder de Habilidade no lugar de +15" (o +50 de Vida igual ao base não conta), desligar no Catálogo desliga na forja; console limpo.
- Fase 3, T9 — totais de atributos e eficiência (captura `docs/screenshots/fase3-tarefa9-totais.png`) — aprovado pelo Leo em 13/09/2026, commit "F3-T9". `effectiveAttributes(item)`: base → Mestre Forjador (total do catálogo) → Ápice (valor extraído; prioridade quando os dois mexem no mesmo atributo). `sumAttributes` soma por atributo separando fixos de percentuais; valores vindos do MF em latão, do Ápice em brasa clara. Cada bancada com itens ganha a placa "Atributos da caixa" (lista + eficiência) e abaixo da barra da build entra a placa "Atributos da build · N itens" com a eficiência geral e uma nota dizendo quais interruptores estão ligados. `goldEfficiency` = soma de `costAnalysis.goldValueTotal` ÷ soma dos preços efetivos (Reembolso muda), itens sem `goldValueTotal` ficam de fora e são contados "(N sem análise)"; MF/Ápice não alteram o valor em ouro (sem dado — docs/propostas_capitulo1.md). Recalculado a cada renderBuild (adicionar, mover, remover, trocar build, interruptores). Testado no navegador embutido: caixa Gume + Lacre + Sentinela = 105,4% (conferido à mão) com "1 sem análise"; MF leva Dano de Ataque a 103,57; Ápice leva PdH a 55; Reembolso muda a eficiência geral para 101,8%; console limpo.
- Fase 3, T10 — Total geral sem as caixas Opcionais (captura `docs/screenshots/fase3-tarefa10-total.png`) — aprovado pelo Leo em 13/09/2026, commit "F3-T10". FASE 3 CONCLUÍDA em 13/09/2026. O resumo da barra passa a somar só as caixas Comum e Prioridade, com o rótulo "Total (sem opcionais)" e title explicando; decisão tomada pela lógica que o Leo deu ao remover o total Opcional (opcional não conta) — reverter é uma linha. Testado no navegador embutido: Gume (3.500g) + Cristal de Rubi (400g) + Armadura de Espinhos opcional (2.450g) → Total (sem opcionais) 3.900g; console limpo.

### Fase 2 — Catálogo (loja medieval)
- Fase 2, T1 — esqueleto na paleta A (captura `docs/screenshots/fase2-tarefa1-esqueleto-loja.png`) — aprovado pelo Leo em 13/09/2026, commit "F2-T1". Um bloco CSS novo no fim do <style> (209 linhas) com variáveis --loja-*, fundo de madeira escura com frisos, cabeçalho em tábuas, abas como placas de latão, busca/ordenação/painel de filtros em papel com a textura 02 (multiply), chips de tinta com ativo em cera, caixas de marcação de tinta/cera, contagem e cabeçalhos de grupo escritos na madeira; fontes IM Fell English SC + Alegreya adicionadas ao link do Google Fonts. Cards e Build intactos. Testado: 225 cards, filtros funcionando (Distribuído = 5), fontes carregadas, console limpo.
- Fase 2, T2 — card em papel (captura `docs/screenshots/fase2-tarefa2-card-papel.png`) — aprovado pelo Leo em 13/09/2026, commit "F2-T2". CSS no bloco da loja: papel com textura 01 (multiply), faixa vertical de 6px na cor da classe (AD laranja, AP roxo, Vitalidade verde; Garrabrasa dividida), selo de cera com o nome do tier no canto inferior direito (cores por tier nas variáveis --tier-*; Starter e Básico em tinta), nome em IM Fell English SC 16px limitado a duas linhas, inglês + classe em itálico com reticências, preço em cera fixo à direita, atributos com travessão, ícone com moldura de tinta; hover intocado (só cores: brilho de latão, papel mais claro). Card de 196px com faixa inferior de 22px reservada ao selo. JS: `--class-color` por card (CLASS_CSS) e " · classe" no nome em inglês; a marca "Mestre Forjador" saiu do card a pedido do Leo (o filtro lateral e o modal continuam). Medido no navegador embutido: 225 cards, 0 cortados, 0 selos sobre texto, console limpo.
- Fase 2, T3 — abas de Classe (captura `docs/screenshots/fase2-tarefa3-abas-classe.png`) — aprovado pelo Leo em 13/09/2026, commit "F2-T3". Régua de madeira acima da contagem com quatro etiquetas de papel: Todos · AD · AP · Vitalidade (rótulos e contagens lidos de `meta.itemClass`/`itemClass.core`; a ativa é preenchida com a cor da classe, TODOS em tinta). `state.classe` entra em `itemMatchesFilters` antes dos demais filtros e combina com eles; "Limpar filtros" volta a Todos. Filhote de Garrabrasa ("AD e AP") conta e aparece em AD e em AP (82/62). Testado no navegador embutido: Todos 225, AD 82, AP 62, Vitalidade 82, Vitalidade + chip = 79, limpar = 225, console limpo.
- Fase 2, T4 — filtros novos (captura `docs/screenshots/fase2-tarefa4-filtros-novos.png`) — commit "F2-T4" (Leo seguiu para o modal sem objeção em 13/09/2026). Quatro grupos novos no painel: **Tipo de dano** (os valores "Dano Físico/Mágico/Verdadeiro" de `meta.specialEffects`), **Custo** (faixas Grátis · até 500 · 501–1000 · 1001–2000 · 2001–3000 · acima de 3000, pelo preço efetivo, logo sensível ao Reembolso), **Eficiência de ouro** (faixas 120%+ · 100–119 · 80–99 · <80 · sem análise; `itemEfficiency` = `costAnalysis.efficiencyBase` do catálogo, ou `goldValueTotal ÷ cashback.netGold` com Reembolso ligado — regra h do Leo; null = sem análise) e **Atributo** (chips de todos os atributos concedidos, recolhido; botões "Maior primeiro / Menor primeiro" ordenam pelo primeiro chip marcado e sincronizam o seletor do topo). Seletor de ordenação ganhou "Eficiência de ouro: maior/menor primeiro" (sem análise vai para o fim, apagado, com a linha "Eficiência de ouro: sem análise" no card; com análise mostra o %). Interpretação de "maior e menor atributo": ordenação pelo atributo escolhido dentro do grupo Atributo — confirmar com o Leo. Testado no navegador embutido: Dano Verdadeiro 10; acima de 3000g 26; ≥120% 41 (60 com Reembolso); sem análise 15; Vida 77 + Maior primeiro → Warmog 1000; eff-desc → Elixir da Feitiçaria 380% primeiro, Sentinela Invisível "sem análise" por último, 15 apagados; Limpar zera tudo; console limpo.
- Fase 2, T5 — modal em pergaminho (captura `docs/screenshots/fase2-tarefa5-modal-pergaminho.png`) — aprovado pelo Leo em 13/09/2026, commit "F2-T5". Papel com textura 03, faixa do tier no topo, ícone do item (72px, moldura de tinta) ao lado do título com classe · tier · região (pedido do Leo), título IM Fell, rótulos Cinzel, chips de receita como etiquetas de tinta. Seções novas: **Mecânica** dentro de cada habilidade (`abilities[].mechanics`, com o tipo da habilidade ao lado do nome), **Análise de custo** (`costAnalysis.goldValue`, `efficiency` e, com Reembolso ligado, "Com Reembolso X% (valor ÷ líquido)"), **Ápice** (`apex`), **Notas de modo** (`aramNotes`/`mayhemNotes`), nota de atributos (`attributesNote`) quando não há atributos (Stat Bonus). Referência de receita fora do catálogo aparece como etiqueta tracejada sem link (Armaguarda Despedaçada; "Missão de Suporte — por transformação"). Atalho `#item=<slug>` na URL abre o item ao carregar. Testado no navegador embutido: Tomo Amplificador (17 destinos, 1 sem link), Stat Bonus (nota + mecânica), Gume do Infinito com Reembolso (103,57% base → 112,0%), Atlas Mundial; console limpo. Lição: navegar só trocando o #hash não recarrega a página no navegador embutido — usar ?query para forçar.
- Fase 2, T5b — texturas sorteadas (pedido do Leo, 13/09/2026; captura `docs/screenshots/fase2-tarefa5b-texturas-sorteadas.png`): toda superfície de papel lê `--tex-pick`; card = sorteio estável pelo slug (`paperFor`), modal = sorteio a cada abertura, busca/painel/abas = sorteio ao carregar. Medido: 63/84/78 cards por textura, console limpo. docs/direcao_de_arte.md atualizado (texturas não são mais fixas por função). Commit "F2-T5".
- Fase 2, T6 — Shift = ficha completa no card (captura `docs/screenshots/fase2-tarefa6-shift-ficha.png`) — aprovado pelo Leo em 13/09/2026, commit "F2-T6". Com Shift segurado, o card sob o mouse abre por cima dos vizinhos (380px, para a esquerda quando está na borda direita da grade) com: todos os atributos (`raw`), habilidades (nome — descrição), Ficha (eficiência do catálogo e com Reembolso, valor em ouro, Reembolso, Mestre Forjador, custo de combinação, modos, condição). Soltar o Shift (ou trocar de janela) volta ao resumo; a grade não se mexe. Ficha montada uma vez por card, sob demanda. Atalho `#ficha=<slug>` deixa a ficha aberta ao carregar (captura). Testado no navegador embutido com keydown/keyup sintéticos: abre e fecha, borda direita respeitada, console limpo.
- Fase 2, T7 — clique marca para o Build (captura `docs/screenshots/fase2-tarefa7-marcar-build.png`) — aprovado pelo Leo em 13/09/2026, commit "F2-T7". Clique no card põe o item na build ativa (categoria selecionada, senão a primeira; sem categoria cria "Da loja"); clique num card já marcado tira o item. Card marcado: papel opaco um tom mais escuro, ícone e texto a .4 dessaturados (o papel não fica transparente porque os raios do hover atrás do card vazariam — apontado pelo Leo), carimbo "NO BUILD" em cera; a aba Build ganha contagem em cera. O pergaminho abre pelo clique no ícone do item (cursor de lupa) ou com Shift+clique; o card tem title explicando. `refreshInBuildMarks()` sincroniza Catálogo ↔ Build e é chamada pelo `renderBuild()` (remover/limpar/trocar de build reflete no Catálogo). Captura do disco feita com um perfil headless semeado (scratchpad/seed_build.html, fora do repositório). Testado no navegador embutido: marcar 4, desmarcar 1, modal pelo ícone e por Shift, aba Build com a categoria "Da loja" e 3 tiles, console limpo. Nota: window.confirm do "Limpar build" não roda no navegador embutido (limitação dele, como o prompt).
- Fase 2, T8 — caixa Ápice (captura `docs/screenshots/fase2-tarefa8-apice.png`) — aprovado pelo Leo em 13/09/2026, commit "F2-T8". Caixa "Ápice: itens nos status máximos" abaixo do Reembolso, com contagem 102 (`hasApex` = `apex` diferente de "igual ao base"). Ligada (T8b, correção do Leo: o ápice SUBSTITUI os atributos, não fica ao lado): `apexStatsOf` extrai do texto `apex` só os valores "+N de <atributo>" com sinal e nome que o catálogo já usa em attributes[].stat (regra conservadora: exclui cura, regeneração por segundo e totais sem sinal — Anel de Doran e Poção com Refil não extraem nada); `apexAttributes` funde com os atributos do item (valor do ápice no lugar do base, marcado com ↑ em cera; atributo só do ápice é acrescentado). Card: lista fundida + linha "ÁPICE <condição>" (texto antes do primeiro ":"); modal: lista fundida com "base +N" ao lado; ficha do Shift: lista fundida. Item cujo ápice não tem valor extraível mostra o texto do ápice no card. Medido: 102 com ápice ≠ base, 21 cards com atributos substituídos, 75 só texto. Provisório até o Capítulo 1 fornecer `apexStats` (docs/propostas_capitulo1.md); os "igual ao base" ficam como estão.
- Fase 2, T7b — correção do Leo (13/09/2026): marcar do Catálogo NÃO é automático. O clique só põe o item se houver uma categoria da Build selecionada à mão; sem seleção, o clique abre o pergaminho do item (segunda correção do Leo; o aviso foi removido) e o cursor vira lupa em todos os cards. Seletor "Marcar em:" ao lado da contagem lista as categorias da build ativa (com quantidade) e define/reflete `build.selectedCatId`, a mesma seleção da aba Build. A categoria "Da loja" automática foi removida. Testado no navegador embutido: sem categoria → abre o pergaminho da Coletora, 0 marcados, cursor zoom-in; com categoria escolhida → marca, cursor pointer.
- Fase 2, T8c — modos de exibição (pedido do Leo, 13/09/2026; capturas `docs/screenshots/fase2-tarefa8c-nomes.png` e `fase2-tarefa8c-so-icones.png`): botões "Cards · Nomes · Ícones" na linha da contagem. Nomes = ícone + nome (sem inglês, preço, atributos ou selo; colunas de 170px, 84px de altura); Ícones = mosaico de 78px só com o ícone e a faixa de classe. Mesmo DOM: hover, clique, carimbo, Shift (a ficha abre completa mesmo nesses modos) e Ápice continuam funcionando. Escolha lembrada em localStorage (`catalogo.view`). Commit "F2-T8". Commit "F2-T8". Conta no grupo Runas e no Limpar filtros. Atalho `#apice` liga ao carregar. Eficiência no Ápice não muda (sem dado estruturado — docs/propostas_capitulo1.md). Testado no navegador embutido: 102 cards, banner, limpar zera, console limpo.
- Fase 2, T9 — animação de clique por tier (captura `docs/screenshots/fase2-tarefa9-animacoes-tier.png`, quadros congelados via `#animacoes`) — aprovado pelo Leo em 13/09/2026, commit "F2-T9". Mapa `TIER_ANIM` (nomes de tier do catálogo → classe): Lendário e Lendário (Evolução) = cera (selo incha, derrete e reassenta, com brilho de latão no papel); Starter e Básico = carimbo (card afunda, selo bate vindo de cima); Épico e Épico (Evolução) = tinta (mancha azul-tinta se espalha a partir do ponto do clique, via ::after e --mx/--my); Consumível = página vira (rotateY na borda esquerda); Trinket = sino (balanço + anel de latão no ícone); Distribuído = moeda (ícone gira 360° no eixo Y); Bota = bate no chão (salto e achatamento); Evolução = brasa (ícone e faixa acendem em laranja). Só CSS/JS sobre o DOM, 350–750 ms, classe removida após 800 ms, reinicia se clicar de novo, desligada com prefers-reduced-motion. Dispara em qualquer clique no card (marcar ou abrir o pergaminho). Testado no navegador embutido: 11 tiers com a animação certa, 0 classes residuais, console limpo.
- Fase 2, T9b — pergaminho animado (pedido do Leo, 13/09/2026): ao abrir, o overlay esmaece e o pergaminho desenrola de cima (scaleY .55 → 1, 420 ms); por cima entra o toque do tier com a mesma classe `TIER_ANIM` no #modal: cera = brilho de latão pulsando no ícone; carimbo = pergaminho afunda e volta; tinta = mancha a partir do canto do ícone (::after); página = entra virando pela borda esquerda; sino = balanço; moeda = ícone gira; bota = batida; brasa = ícone acende. Navegar entre receitas (chips) refaz a abertura com o tier do novo item. Testado no navegador embutido: 11 tiers com as animações certas no modal/ícone, replay ao navegar (Gume → Espada G. p. C. = carimbo), console limpo. Commit "F2-T9".
- Fase 2, T10 — tela de entrada (captura `docs/screenshots/fase2-tarefa10-entrada.png`) — aprovado pelo Leo em 13/09/2026, commit "F2-T10". FASE 2 CONCLUÍDA em 13/09/2026. Overlay de madeira escura cobrindo a loja ao carregar, com a placa de papel (textura sorteada) pendurada em duas correntes, balanço leve ao entrar, título em IM Fell, frase em itálico, botão de cera "Entrar na loja" (relevo, afunda ao clicar) e nota com o patch lido de `meta.patch`. Clique: a placa sobe girando e o overlay esmaece (900 ms), revelando o Catálogo; gancho comentado para `Som.play("catalog:open")` na Fase 4. Links diretos (#item, #ficha, #apice, #animacoes) entram na loja sem a placa. Botão recebe foco ao carregar (Enter funciona em navegador real). Respeita prefers-reduced-motion. Testado no navegador embutido: placa visível ao carregar, clique fecha (saindo → fechada, display none), 225 cards atrás, link direto abre o Gume com a entrada fechada, console limpo.

### Fase 0 — Fundação de dados
- T1 — Assets conferidos e commit inicial (`docs/screenshots/fase0-tarefa1-baseline.png`): 82 mp3 batendo um a um com docs/mapa_de_audio.md; 3 texturas; captura de baseline do index.html antigo (227 itens embutidos). Commit ed64738, aprovado pelo Leo.
- T2 — catalog.js regerado e integridade conferida (sem captura: tarefa de dados). Python 3.12.10 via winget; gerador rodado com `PYTHONUTF8=1` (o `print` final quebra no console cp1252; arquivo já escrito antes; script intocado). Saída idêntica ao commitado: 225 itens, 7 avisos, 0 linhas não reconhecidas. Checagem própria: 225 itens, sem duplicatas, sem campo obrigatório vazio, preços e somas de receita batendo, todo Lendário com masterwork. Os achados (Shattered Armguard ausente, Atlas → Missão de Suporte, id de Stat Bonus, Atma's Reckoning) são decisões já tomadas no Capítulo 1 — registradas em docs/decisoes_capitulo1.md, não como pendência.
- T3 — index.html migrado para data/catalog.js (captura `docs/screenshots/fase0-tarefa3-catalogo-migrado.png`), aprovado pelo Leo em 13/09/2026, commit "F0-T3". O que mudou: CATALOG embutido (227 itens) e ITEM_ICONS base64 removidos → `<script src="data/catalog.js">`; index.html de 1,9 MB para 130 KB. Adaptador de esquema no topo do último script: chave do item = `slug` (builds salvas com a chave antiga sem apóstrofo são traduzidas ao carregar; item que não existe mais sai da build); `components`/`buildsInto` resolvidos por nameEn em `componentRefs`/`buildsIntoIds` (peça repetida vira ×N, `combineCost` na última peça, referência fora do catálogo não é exibida); tiers, ordem de tier e modos lidos do catálogo (rótulos agora são os do Capítulo 1: "Starter", "Trinket", "Evolução"...); ícone por `iconUrl` do Data Dragon com reserva de abreviação se a imagem falhar; `masterwork.raw`, `cashback.raw` e `cashback.netGold` nos lugares dos campos antigos. Testado no navegador embutido via servidor local (http.server 8765, config em .claude/launch.json, ignorado no git): 225 cards, console limpo, 0 ícones quebrados (225/225 URLs respondem 200), modal do Atma com "Cinto do Gigante ×2", filtro Lendário 114, Mestre Forjador 109, Reembolso 3000g→2775g (11 itens de transformação gratuita têm cashback.raw = "none" sem netGold: tratados como não reembolsáveis, badge 109), Build por clique 3450g, Calculadora com preços do catálogo, migração de build antiga OK. Do disco (file://, Chrome headless): 225 cards.
- T3b — pedido do Leo (13/09/2026): removido o total de ouro "Opcional" do resumo da Build (itens opcionais não contam para isso). "Total geral" continua somando tudo — confirmar com o Leo se deve excluir os opcionais também. Aprovado em 13/09/2026, commit "F0-T3b".
- T4 — Pages testado (`docs/screenshots/fase0-tarefa4-pages.png`): deploy no ar 30 s após o push; index.html, data/catalog.js, textura, áudio e .nojekyll respondem 200 em https://dksaxton.github.io/montador_de_itens_lol/; DOM do site publicado com 225 cards e contador 225 (Chrome headless — o navegador embutido não tem permissão para o domínio github.io). Do disco (file://) também 225. FASE 0 CONCLUÍDA em 13/09/2026.

## 3. Em andamento
Fase 8: T1 feita (14/09/2026). Bloqueada em dois itens do Leo: print do menu "Publicar" do Deadlock (antes da T3) e projeto no Supabase (URL + chave anon, para a T2). Nada em andamento. Fase 8 T1–T5 aprovadas e publicadas (14/09/2026). Publicações de teste apagadas do servidor em 14/09/2026 a pedido do Leo (o site começa vazio). Pendências só a pedido: autor opcional na publicação; limpeza automática dos eventos (docs/servidor/limpeza.sql). Fila: Fase 8 — Builds publicadas (ver seção 4), começando pela T1 (link com a build no #hash) quando o Leo pedir; a pausa para o print do menu de publicação do Deadlock continua obrigatória antes de desenhar a publicação.

Dúvidas do Leo em 14/09/2026, respondidas como avaliação (sem implementar):
1. Busca de builds publicadas com ID automático e nome (como o Deadlock): precisa de um servidor/banco compartilhado — o app é estático no Pages. Caminhos: (a) link com a build codificada no `#hash` (sem backend, sem busca, dá para fazer já); (b) backend gratuito escolhido pelo Leo (ex.: Supabase/Firebase) com ID gerado lá e busca por nome/ID — feature de um Capítulo 3.
2. Subir a build para os "arsenais" (conjuntos de itens) do cliente do LoL: a página não consegue falar com o cliente (API local com senha no lockfile, certificado próprio, CORS). Leo escolheu exportar o JSON (14/09/2026) → Fase 7, T1. Push direto exigiria um programa local (script Python lendo o lockfile e chamando /lol-item-sets), fora da página.
3. Publicar builds com ID e busca (Leo, 14/09/2026: "como que a gente vai fazer isso?"): plano proposto — Fase 8 (por último, decisão do Leo em 14/09/2026): T1 link com a build no `#hash` (sem servidor); T2 Leo cria projeto gratuito no Supabase e cola URL + chave pública em `data/servidor.js` (gitignored? não: chave pública é pública por desenho; regras de acesso no banco); T3 aba "Publicadas" na biblioteca: publicar (ID curto gerado no banco, nome customizado, modo, autor opcional), buscar por texto ou ID, abrir e copiar para a biblioteca local; editar/apagar só com o segredo guardado no navegador de quem publicou. Sem login, sem dado pessoal.

## 4. Próximos passos
Ordem das fases (Leo, 14/09/2026: "a fase de inserir a build no cliente do LoL é acima da do servidor, vamos deixar a do servidor por último"):
Fase 7 — Build no cliente do LoL: CONCLUÍDA (T1 exportar · T2 campeão via Data Dragon · T3 lista de Arsenais + forja · T4 importar Arsenal.json · T5 marcadores). Só a pedido: vários campeões por build; programinha local para enviar direto ao cliente.
Fase 8 — Builds publicadas. Especificação do Leo (14/09/2026, com três prints do "Explorar guias" do Deadlock):
- **Nome**: a tela chama-se "Builds" (não "Explorar guias" nem "Arsenais"). Abas **Minhas** e **Públicas**. Busca por nome ou ID. Botão **Filtrar por marcador** mostrando todos os marcadores; os de habilidade só aparecem quando um campeão está no filtro. Lista à esquerda (retrato, nome, etiqueta "Popular hoje/da semana", 3 marcadores, coração de favorito), detalhe à direita.
- **Efeito de entrada**: a tela se materializa "como pingos de chuva pintando rapidamente a aba" (só CSS/JS sobre o DOM).
- **Minhas** (padrão): as builds do usuário, com marca de **desatualizada** e **favoritas** (favoritas ficam sempre na lista em vez de sumirem com o tempo). Detalhe: descrição resumida, **versão** (cada publicação da mesma build soma 1 e grava data e hora da última atualização), **ID da build**, itens (exceto opcionais), botões **Copiar e editar · Editar · Publicar · Excluir**.
- **Publicar**: envia ou atualiza a build no site. Só permitido com itens, nome, **descrição** (campo novo na build) e os **3 marcadores** preenchidos.
- **Públicas**: top 10 populares do dia e da semana (mais copiadas e visualizadas), depois as mais recentes (recentes fora do top 10 podem levar "Popular da semana"); seletor de quantidade 10 / 20 / **50 (padrão)** / 100. Ao selecionar uma pública: nome, marcadores, versão, ID, última atualização, descrição (com links), itens, botão **Copiar e editar**.
- **Fluxo de publicar** (Leo, 14/09/2026, com print): não há menu. Clicar em Publicar valida, mostra a placa "Publicando a build — Aguarde" e, quando todo o processo termina, a janela "Build publicada com sucesso". A pausa para o print está cumprida.
- **Servidor**: Leo criou a conta no Supabase pelo GitHub (14/09/2026). Preparados `docs/servidor/README.md` (passo a passo: criar projeto, rodar o SQL, pegar URL + chave anon, preencher `data/servidor.js`) e `docs/servidor/supabase.sql` (tabelas `builds` e `eventos`, view `builds_publicas` com pop_dia/pop_semana, funções `publicar_build` com validação + ID curto + versão, `apagar_build`, `registrar_evento`; RLS ligado, chave pública só vê a view e as funções; segredo do autor guardado como hash). `data/servidor.js` criado vazio.
- Ordem prevista: **T1** (sem servidor) tela "Builds" com abas Minhas/Públicas, layout lista + detalhe, campo Descrição na forja, busca e filtro por marcador locais, efeito de chuva, link com a build no `#hash` · **T2** Leo cria o projeto no Supabase, eu passo o SQL (tabelas builds/cópias/visualizações/favoritos) e a chave pública vai em `data/servidor.js` · **T3** Publicar com validação, ID, versão e data — **pausa obrigatória antes: pedir ao Leo o print do menu que abre ao clicar em Publicar no Deadlock** · **T4** Públicas com top 10 dia/semana, recentes, etiquetas, quantidade, favoritos e "desatualizada" · **T5** detalhe da pública com Copiar e editar (conta cópia) e contagem de visualizações.

Possíveis continuações, só com pedido do Leo:
- Quando o Capítulo 1 fornecer os campos de docs/propostas_capitulo1.md (valor em ouro do bônus do Mestre Forjador, ápice estruturado, fragmentos do Stat Bonus): ligar a eficiência ao Mestre Forjador e ao Ápice, desligar a extração de texto do ápice, permitir escolher o fragmento do Atributo adicional na build.
- Rótulo "Prioridade" → "Obrigatória" (uma linha), se o Leo quiser.
- Ajustes finos de som (constantes em Som.LOJISTA e ganhos por evento).

Histórico das especificações por fase (mantido para referência):
Fase 3 — Build (forja). Além do CLAUDE.md:
  e. Eficiência de ouro por categoria da build: soma de `costAnalysis.goldValueTotal` ÷ soma de `priceGold` dos itens da categoria (só aritmética sobre os campos do catálogo; itens sem análise ficam de fora e a caixa avisa).
  f. Atributo adicional na build: escolher qual fragmento o item concede exige lista estruturada de fragmentos (stat, valor, nível). Hoje é texto em `mechanics` → propor ao Capítulo 1 um campo estruturado antes de implementar; o Forjador não digita a lista à mão.
  g. Visão Ápice na build lê o mesmo campo `apex` (texto), como no CLAUDE.md.
  i. Dois modos na Build (Leo, 13/09/2026): **visualização** (padrão ao abrir — a build como ficha limpa: sem alças de arrastar, sem botões de remover/redimensionar, campos de nome/descrição não editáveis, observações só ao passar o mouse) e **edição** (interruptor "Editar" liberando drag-and-drop, caixas, observações, remover). Biblioteca, exportar/importar e os interruptores Mestre Forjador/Reembolso/Ápice funcionam nos dois modos.
  h. Eficiência de ouro recalculada pelos interruptores (Leo, 13/09/2026): com Reembolso = `goldValueTotal ÷ cashback.netGold` (dá para fazer já); com Mestre Forjador e Ápice depende de campos numéricos que o catálogo não tem → docs/propostas_capitulo1.md. Vale no Catálogo (Fase 2) e na eficiência por categoria da Build (Fase 3).
Fase 4 — Som e lojista · Fase 5 — Calculadora e biblioteca · Fase 6 — Publicação e QA

## 5. Decisões tomadas
- Plataforma: Claude Code na pasta do repositório DKsaxton/montador_de_itens_lol; deploy por GitHub Pages (main, raiz, .nojekyll). Pages ATIVO desde 13/09/2026 (Deploy from a branch, main, / root): https://dksaxton.github.io/montador_de_itens_lol/
- Dados: data/catalog.js gerado, nunca editado à mão; ícones por URL do Data Dragon pelo ID.
- Escopo: só os 225 itens do Capítulo 1. Sem itens removidos, novos ou campeões.
- Animações de clique por tier; áudio por núcleo (hover/clique), por tier (Lendário no Build) e por item (falas raras).
- localStorage permitido (app roda em navegador real).
- Capturas de tela e testes de clique: pelo navegador embutido do Claude Code (pedido do Leo, 13/09/2026). O navegador embutido não carrega arquivos relativos via file:// (inlina o HTML como snapshot), então os testes rodam pelo servidor local `montador` (.claude/launch.json, http.server na porta 8765); o PNG em docs/screenshots/fase-tarefa-descricao.png é gerado pelo Chrome headless direto do disco (1440×900), que também prova o caso "abrir com dois cliques".
- Contagem de áudio: 82 arquivos (o mapa_de_audio.md é a referência).
- Python: 3.12.10 instalado em %LOCALAPPDATA%\Programs\Python\Python312 (winget, autorizado pelo Leo em 13/09/2026). Rodar o gerador com `PYTHONUTF8=1` para evitar erro de encoding no console.
- O Catálogo está fechado no Capítulo 1: achados da integridade que já foram decididos lá ficam em docs/decisoes_capitulo1.md e não são reportados de novo. Nunca corrigir Catalogo_Itens_LoL.md nem o gerador.

## 6. Bloqueios / dúvidas em aberto
- Propostas de adição ao Capítulo 1 em docs/propostas_capitulo1.md (valor em ouro do bônus do Mestre Forjador, ápice estruturado, fragmentos do Stat Bonus). Sem elas, o app mostra o texto do catálogo e só recalcula a eficiência com Reembolso.
- Navegador embutido do Claude Code não suporta window.prompt() (usado hoje pela observação por item na Build, duplo clique). Em navegador real funciona; a Fase 3 troca o prompt por edição na própria tela.
- Nenhuma pendência de dados: os achados da integridade são decisões fechadas do Capítulo 1 (docs/decisoes_capitulo1.md). Única dúvida de fonte aberta lá: lentidão do Scorchclaw Pup (2s vs 3s), marcada [A CONFIRMAR].
- Semântica de `shopkeeper_tag_open_*`: proposta ao Leo em 13/09/2026 — tocar ao trocar de aba de Classe (AD/AP/Vitalidade). Aguardando confirmação (Fase 4).
